import NotesClient from "./NotesClient";

export const metadata = {
    title: "Knowledge Base | ArborDesk",
    description: "Your team's collective brain.",
};

export default function NotesPage() {
    return <NotesClient />;
}
