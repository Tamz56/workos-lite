import TrialLabClient from "@/components/workspaces/travel/TrialLabClient";

export const metadata = {
    title: "Green Fineness Trial Lab — Tomato Pot Rainy Season Trial",
};

export default function TrialLabPage() {
    return <TrialLabClient />;
}
